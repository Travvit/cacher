/**
 * Abstract class that describes a Factory.
 */
class Factory {
    /**
     * @returns an object of the type the factory is expected to generate.
     */
    create() {}
}

module.exports = Factory;
